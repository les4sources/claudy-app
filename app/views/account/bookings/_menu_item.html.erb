<% if can? :read, Booking.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :bookings]),
    label: t('bookings.navigation.label'),
  } do |p| %>
    <% p.content_for :icon do %>
      <i class="<%= t('bookings.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
